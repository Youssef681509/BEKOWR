<table class="table table-stripped table-borderless">
        <thead class="table-dark">
            <tr>
                <th>Code</th>
                <th>Date donation</th>
                <th>Date Réception</th>
                <th>Montant</th>
                <th>Devise</th>
                <th>Statut</th>
                <th>Methode de Paiement</th>
                <th>Mdp Donation</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for donation in donations %}
            <tr>
                <td>{{ donation.extCode }}</td>
                <td>{{ donation.dateOfDonation ? donation.dateOfDonation|date('d/m/Y') : '' }}</td>
                <td>{{ donation.dateOfReceipt ? donation.dateOfReceipt|date('d/m/Y') : '' }}</td>
                <td>{{ donation.donAmount }}</td>
                <td>{{ donation.donCurrency }}</td>
                <td>{{ donation.statusOfDonation }}</td>
                <td>{{ donation.paymentMethod }}</td>
                <td>{{ donation.passwordDon }}</td>
                <td>
                    <a href="{{ path('app_donation_show', {'id': donation.id}) }}">show</a>
                    <a href="{{ path('app_donation_edit', {'id': donation.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="13">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>