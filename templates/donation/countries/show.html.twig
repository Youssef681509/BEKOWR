{% extends 'base.html.twig' %}

{% block title %}Countries{% endblock %}

{% block body %}
    <h1>Countries</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ country.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ country.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ country.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_donation_countries_index') }}">back to list</a>

    <a href="{{ path('app_donation_countries_edit', {'id': country.id}) }}">edit</a>

    {{ include('donation/countries/_delete_form.html.twig') }}
{% endblock %}
